interface:
  display_name: "TeneT HPC"
  short_description: "Operate TeneT cluster jobs safely"
  default_prompt: "Use $tenet-hpc to prepare, monitor, or sync a TeneT Slurm job."

policy:
  allow_implicit_invocation: false
